$(document).ready(function(){
  init();
});

function init() {
  // initInfiniteScroll();
  // initDislike();
  // initGenderSlider();
    AppController.init();
  // bindHandlers();

  ProductController.init();
  AppController.init();
}

function bindHandlers(){

  //bind to like AJAX
  //bind to load more randoms
}
