$(document).ready(function(){
  init();
});

function init() {
  // initInfiniteScroll();
  // initDislike();
  // initGenderSlider();
  // bindHandlers();

  ProductController.init();
  AppController.init();
}

function bindHandlers(){

  //bind to like AJAX
  //bind to load more randoms
}
