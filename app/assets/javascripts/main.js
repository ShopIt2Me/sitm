$(document).ready(function(){
  init();
});

function init() {
  // initInfiniteScroll();
  // initDislike();
  // initGenderSlider();
  // initToolTips();
  // bindHandlers();

  // AppController.init();
  ProductController.init();
}

function bindHandlers(){

  //bind to like AJAX
  //bind to load more randoms
}
